import wixPayBackend from 'wix-pay-backend';

export function createPaymentObj(ids, amounts) {
    return wixPayBackend.createPayment({
        items: [{
            name: "Product Name",
            price: 1.99
        }],
        amount: 1.99
    });
}